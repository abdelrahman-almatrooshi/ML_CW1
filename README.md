# ML_CW1
ML CW1

## Running the model

From the repo root, with training data in `data/`:

```bash
python src/train_model.py
```

Expects `data/CW1_train.csv` and `data/CW1_test.csv`. Writes predictions to `outputs/CW1_submission_k21190308.csv`.
